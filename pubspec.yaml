name: m3xpressive_indicators
description: Material 3 Expressive wavy progress and loading indicators — circular and linear, determinate and indeterminate — for Flutter SDKs older than the ones the official widgets require.
version: 0.1.0
homepage:
repository:

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
